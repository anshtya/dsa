#include <iostream>
using namespace std;

// palindrome string  // T.C -> O(n)
// bool isPalin(string s, int r , int l = 0){ //base condition :- if one char is present
//     if( l >= r)       
//         return true;
    
//     if( s[l] != s[r] )
//         return false;
    
//     return isPalin(s, r - 1, l + 1);

// } 

//subsequences (powerset) of string  // T.C -> O(2^n)
// void PowerSet(string s, string cur = "", int i = 0){
//     if( i == s.length()){
//         cout << cur << " ";
//         return;
//     }
//     PowerSet(s, cur + s[i], i + 1);
//     PowerSet(s, cur, i + 1);
// }

// permutations of given string

int main(){
    
    return 0;
}